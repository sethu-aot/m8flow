name: m8flow-keycloak CI/CD
# =============================================================================
# Triggers:
#   PR to 'main'           → CI only (lint, test, SAST, Trivy, build check)
#   Push/merge to 'main'   → CI only (build + push image to Docker Hub)
#   workflow_dispatch       → CD only (promote pre-built image to dev/demo/qa)
#
# ALL deployments are manual via workflow_dispatch (promotion-only).
# QA and DEMO MUST receive a pre-built image_uri — no fresh builds on dispatch.
# =============================================================================

on:
  push:
    branches:
      - main
    paths:
      - "m8flow-keycloak/**"
      - "keycloak-extensions/**"
      - "docker/m8flow.keycloak.Dockerfile"
      - ".github/workflows/keycloak-cicd.yaml"

  pull_request:
    branches:
      - main
    paths:
      - "m8flow-keycloak/**"
      - "keycloak-extensions/**"
      - "docker/m8flow.keycloak.Dockerfile"
      - ".github/workflows/keycloak-cicd.yaml"

  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment to deploy to"
        required: true
        default: "dev"
        type: choice
        options:
          - dev
          - demo
          - qa
      image_uri:
        description: "Pre-built image URI to promote (e.g. m8flow/m8flow-keycloak:dev-a1b2c3d)"
        required: true
        type: string

# Grant the permissions the reusable CI orchestrator needs (security-events,
# actions, packages, pull-requests, id-token). The CD orchestrator handles its
# own AWS OIDC via id-token: write which is also covered here.
permissions:
  contents: read
  security-events: write
  actions: write
  packages: write
  pull-requests: write
  id-token: write
  deployments: write

concurrency:
  group: keycloak-cicd-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  ci:
    name: Keycloak CI Pipeline
    if: github.event_name != 'workflow_dispatch'
    uses: sethu-aot/devx-reusable-workflows/.github/workflows/ci-orchestrator.yaml@6110a5e3a3594b36c576e9e3e82f5ff8f105c303
    with:
      config_path: keycloak-extensions/devx-ci.yaml
      auto_merge: false
      push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    secrets:
      registry_username: ${{ secrets.DOCKERHUB_USERNAME }}
      registry_password: ${{ secrets.DOCKERHUB_TOKEN }}
      sonar_token: ${{ secrets.SONAR_TOKEN }}

  cd-dev:
    name: Deploy Keycloak → DEV
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'dev'
    uses: sethu-aot/devx-reusable-workflows/.github/workflows/cd-orchestrator.yaml@6110a5e3a3594b36c576e9e3e82f5ff8f105c303
    with:
      environment: dev
      image_uri: ${{ github.event.inputs.image_uri }}
      config_path: keycloak-extensions/devx-config.yaml
    secrets: inherit

  cd-demo:
    name: Deploy Keycloak → DEMO
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'demo'
    uses: sethu-aot/devx-reusable-workflows/.github/workflows/cd-orchestrator.yaml@6110a5e3a3594b36c576e9e3e82f5ff8f105c303
    with:
      environment: demo
      image_uri: ${{ github.event.inputs.image_uri }}
      config_path: keycloak-extensions/devx-config.yaml
    secrets: inherit

  cd-qa:
    name: Deploy Keycloak → QA
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'qa'
    uses: sethu-aot/devx-reusable-workflows/.github/workflows/cd-orchestrator.yaml@6110a5e3a3594b36c576e9e3e82f5ff8f105c303
    with:
      environment: qa
      image_uri: ${{ github.event.inputs.image_uri }}
      config_path: keycloak-extensions/devx-config.yaml
    secrets: inherit
